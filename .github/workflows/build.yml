name: build

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  call-laravel:
    uses: DerManoMann/openapi-router/.github/workflows/laravel.yml@ci
  call-lumen:
    uses: DerManoMann/openapi-router/.github/workflows/lumen.yml@ci
  call-slim:
    uses: DerManoMann/openapi-router/.github/workflows/slim.yml@ci
